# 2026-09-04 音色、自由调音与视唱跟随

- 和弦查询的吉他指板及和弦播放共用六弦真实采样，按实际弦及品位变调；停止事件取消播放，采样加载失败明确提示。
- 自由调音改为中央输入固定目标音（例如 E2、D2）；不再随着收音自动更换目标，音分以固定目标计算。
- 视唱图片分析谱行与小节边界，结合校对稿音高匹配符头；手动定位优先。光点按音频时钟切换，取消自动滚屏。
- 定位限制：面向清晰、平直、单谱表乐谱。小节数量不匹配时只显示已定位音符，不伪造跨行位置；部分符头仍需手动校对。本次没有重新训练识谱模型。

## 验证

- `node quality/audio-follow-check.js`、`node quality/tuner-theme-check.js` 通过；相关 JavaScript 语法及 git diff 空白检查通过。
- 浏览器实际载入并解码吉他采样，三音播放及停止通过；变调倍率符合半音公式。自由调音输入 D2 后显示 73.42 Hz。
- 自动图像测试覆盖每行 3 + 2 个不等宽小节、符头匹配、数量不符时的手动定位回退及音频时间边界。
- 未完成真实麦克风和多种实拍乐谱的全面验收，不宣称全部谱面自动精准定位。
- 全量旧检查 `quality/exam-class-check.js` 的“嵌入出题页复用主页会话”失败：检查仍匹配仅 teacher 的旧代码字符串，当前未改动的 teacher.js 已允许 teacher/admin。此次不改动账户相关逻辑。

## 发布文件

- 网站与 Windows 共用同一批前端文件。
- Windows 输出：`desktop-build/releases/audio-follow-20260904/`，两款均包含 offline-omr-engine。
- 上一版备用：`desktop-build/releases/question-bank-full-20260902/`。
